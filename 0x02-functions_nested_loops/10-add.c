#include "main.h"
/**
 * add - Entry point
 * @num1: one of the variables
 * @num2: the second one
 * Return: Always result Success
 */
int add(int num1, int num2)
{
	int result;
	result = num1 + num2;

		return (result);
}
