#include "main.h"

/**
 * print_line - draws a straight line in the terminal
 * @n: character being used
 * Return: Always 0
 */
void print_line(int n)
{
	int a, b;

	if (n > 0)
	{
		_putchar('_');
		_putchar('\n');
	}
	else
		_putchar('\n')
}
