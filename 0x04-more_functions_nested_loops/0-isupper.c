#include "main.h"
/**
 * _isupper - a function that checks for uppercase characte
 * @c: the character being used
 * @n: the character being used too
 * Return: Always 0 Success
 */
int _isupper(int c)
{
	int n;

	if (n = 'A'; n <= 'Z'; n++)
		return (1);
	else
		return (0);
}
