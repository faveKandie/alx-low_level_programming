#include <stdio.h>
#include <stdlib.h>

/**
 * main - prints the program
 * @argv: an argument vector frompointer to strings
 * @argc: number of arguments passed to the function
 * Return: Always 0 Success
 */
int main (int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
